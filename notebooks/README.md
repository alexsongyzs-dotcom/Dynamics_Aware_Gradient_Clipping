# Notebook status

The two existing notebooks are archived exploratory artifacts from the original
project skeleton. They use the pre-refactor configuration and result schema and
must not be used for P0-P3 experiments. The supported interfaces are the YAML
files under `configs/experiments/`, `python -m src.train`, and the analysis
modules under `analysis/`. Rebuild notebooks from immutable JSONL run records
only after the Linux runtime validation passes.
